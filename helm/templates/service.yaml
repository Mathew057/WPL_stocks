apiVersion: v1
kind: Service
metadata:
  name: {{ include "wpl_stocks.fullname" . }}
  labels:
    {{- include "wpl_stocks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wpl_stocks.selectorLabels" . | nindent 4 }}
